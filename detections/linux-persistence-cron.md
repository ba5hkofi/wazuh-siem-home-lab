# Linux Persistence via Cron Job

## Overview
This scenario simulates persistence on a Linux system by adding a cron job that executes periodically.

Persistence is a common attacker technique to maintain access to a compromised system.

---

## Simulation Steps

On the Linux agent:

```bash
crontab -e
